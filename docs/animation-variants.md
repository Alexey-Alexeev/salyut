# Варианты анимации "Посмотри меня!" для карточек товаров

## ✅ Текущий активный вариант: Вариант 1
**Деликатная пульсирующая рамка + бейдж сверху** — реализован и активен

---

## Все доступные варианты

### ✅ Вариант 1: Деликатная пульсирующая рамка + бейдж сверху (АКТИВЕН) ✨
**Описание:** Тонкая пульсирующая рамка по краям карточки + небольшой бейдж в правом верхнем углу с эмодзи и текстом
**Плюсы:** 
- Менее навязчиво
- Не перекрывает контент
- Современный вид
- Легко заметно

**Файл:** `components/product-card.tsx` (строки 310-327)

### Вариант 2: Волна по краям карточки 🌊
**Описание:** Анимация "волны" света, пробегающей по периметру карточки
**Плюсы:**
- Очень деликатно
- Не мешает просмотру
- Уникальный эффект

### Вариант 3: Стрелка-указатель с текстом 👆
**Описание:** Появляется стрелка слева от карточки, указывающая на неё, с текстом "Посмотри меня!"
**Плюсы:**
- Четко указывает на элемент
- Минималистично
- Хорошая читаемость

### Вариант 4: Мягкое свечение (Glow эффект) 💫
**Описание:** Мягкое пульсирующее свечение вокруг карточки с небольшим бейджем
**Плюсы:**
- Элегантно
- Не отвлекает от контента
- Современный тренд

### Старый вариант: Большой центральный блок
**Описание:** Большой центральный блок с градиентом и эмодзи салюта (🎆🎇✨) с вращением
**Плюсы:**
- Очень заметно
- Эффектное

---

## Как переключиться между вариантами

### Шаг 1: Откройте файл
`components/product-card.tsx`

### Шаг 2: Найдите блок анимации
Он находится сразу после открывающего тега `<Card>` (примерно строка 310)

### Шаг 3: Закомментируйте текущий вариант
Найдите активный вариант (Вариант 1) и закомментируйте его

### Шаг 4: Раскомментируйте нужный вариант
В том же файле ниже есть закомментированные блоки с другими вариантами. Раскомментируйте нужный.

### Пример переключения на Вариант 2:

**Было (Вариант 1):**
```tsx
{showAttentionAnimation && (
  <>
    {/* Пульсирующая рамка уже применяется через класс на Card */}
    <div className="absolute top-2 right-2 z-[60]...">
      {/* бейдж */}
    </div>
  </>
)}
```

**Стало (Вариант 2):**
```tsx
{showAttentionAnimation && (
  <div className="absolute inset-0 z-50 pointer-events-none overflow-hidden rounded-lg">
    <div className="absolute inset-0 border-2 border-transparent animate-attention-wave-border"></div>
    <div className="absolute top-2 right-2 bg-gradient-to-br from-orange-500 to-purple-700 rounded-lg px-3 py-2 shadow-xl">
      <p className="text-white font-bold text-sm">🎆 Посмотри меня!</p>
    </div>
  </div>
)}
```

### Шаг 5: Обновите классы на Card (если нужно)
Для Варианта 1 используется класс `animate-attention-border` на Card. Для других вариантов его нужно убрать или заменить.

---

## Рекомендация
**Вариант 1** (текущий) — лучший баланс между заметностью и ненавязчивостью.

Если хотите что-то более заметное → используйте **Старый вариант**  
Если хотите что-то более деликатное → используйте **Вариант 2 или 4**

---

## Тестирование
1. Откройте каталог: `/catalog`
2. Подождите 20 секунд
3. Анимация появится на одной из видимых карточек
4. Проверьте, что анимация не мешает просмотру контента

