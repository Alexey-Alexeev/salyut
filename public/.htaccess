# ===============================
# 🌐 Основные настройки
# ===============================
RewriteEngine On
Options -Indexes

# Разрешённые домены
RewriteCond %{HTTP_HOST} ^(www\.)?salutgrad\.ru$ [NC,OR]
RewriteCond %{HTTP_HOST} ^салютград\.рф$ [NC]
RewriteRule ^ - [L]

# Все остальные — редирект на основной домен
RewriteRule ^(.*)$ https://salutgrad.ru/$1 [L,R=301]

# Принудительно HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301,NE]

# ===============================
# 🔄 SPA fallback (Next.js static export)
# ===============================

# Не переписываем статические ассеты и чанки Next.js
RewriteCond %{REQUEST_URI} ^/_next/ [OR]
RewriteCond %{REQUEST_URI} ^/icons/ [OR]
RewriteCond %{REQUEST_URI} ^/images/ [OR]
RewriteCond %{REQUEST_URI} ^/fonts/ [OR]
RewriteCond %{REQUEST_URI} ^/favicon\.ico$
RewriteRule ^ - [L]

# Fallback только для маршрутов приложения
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]

# ===============================
# ⚙️ Кэширование статических файлов
# ===============================
<IfModule mod_expires.c>
    ExpiresActive On

    # HTML — короткое кэширование для актуальности данных
    <FilesMatch "\.html?$">
        ExpiresDefault "access plus 20 minutes"
        Header set Cache-Control "public, max-age=1200, must-revalidate"
    </FilesMatch>

    # Изображения, шрифты, стили и скрипты — длительное кэширование
    <FilesMatch "\.(js|css|jpg|jpeg|png|gif|svg|ico|webp|woff|woff2|ttf|eot)$">
        ExpiresDefault "access plus 1 year"
        Header set Cache-Control "public, immutable"
    </FilesMatch>

    # По умолчанию — 1 час
    ExpiresDefault "access plus 1 hour"
</IfModule>

# ===============================
# 💨 Gzip / Brotli сжатие
# ===============================
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain text/html text/xml text/css
    AddOutputFilterByType DEFLATE application/xml application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>

<IfModule mod_brotli.c>
    AddOutputFilterByType BROTLI_COMPRESS text/html text/plain text/css text/javascript
    AddOutputFilterByType BROTLI_COMPRESS application/javascript application/json
</IfModule>

# ===============================
# 🛡️ Безопасность и SEO
# ===============================
<IfModule mod_headers.c>
    # Защита от MIME sniffing
    Header always set X-Content-Type-Options "nosniff"

    # Защита от XSS
    Header always set X-XSS-Protection "1; mode=block"

    # Безопасная политика Referer
    Header always set Referrer-Policy "strict-origin-when-cross-origin"

    # HSTS — заставляем браузеры использовать HTTPS
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

    # 🎯 Яндекс.Метрика — разрешаем iframe
    SetEnvIf Referer "^https?://([^/]+\.)?(metrika\.yandex\.(ru|by)|metrica\.yandex\.(com|com\.tr)|webvisor\.com)/" allow_yandex
    Header set X-Frame-Options "SAMEORIGIN" env=!allow_yandex
    Header set Content-Security-Policy "frame-ancestors 'self' https://metrika.yandex.ru https://metrika.yandex.by https://metrica.yandex.com https://metrica.yandex.com.tr https://webvisor.com https://*.webvisor.com"
</IfModule>

# ===============================
# 🔒 Доступ к файлам
# ===============================

# Разрешаем доступ к PWA-манифесту
<FilesMatch "^(manifest\.json|site\.webmanifest)$">
    Require all granted
    Header set Content-Type "application/manifest+json; charset=utf-8"
</FilesMatch>

# Разрешаем доступ к SEO-файлам
<FilesMatch "^(sitemap\.xml|robots\.txt)$">
    Require all granted
</FilesMatch>

# Запрещаем доступ к системным и конфигурационным файлам
<FilesMatch "(\.(env|git|htaccess|lock|config|log|sh|sql))$">
    Require all denied
</FilesMatch>