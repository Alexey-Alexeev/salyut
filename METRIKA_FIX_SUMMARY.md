# Исправление Яндекс.Метрики для SPA - Сводка

## 🎯 Задача

**Проблема**: Яндекс.Метрика не получает данные о просмотрах страниц на SPA-сайте, из-за чего не работает Вебвизор и не собирается статистика.

**Ответ поддержки Яндекс**: Параметр `defer: true` установлен, но данные о просмотрах не передаются. Необходимо настроить отправку метода `hit` при каждом изменении страницы.

## ✅ Выполнено

### 1. Полностью переработан компонент `YandexMetrika`

**Файл**: `components/yandex-metrika.tsx`

**Что изменено**:

#### До:
- Сложная логика с множественными задержками и проверками
- Первый hit отправлялся с фиксированной задержкой 300ms
- Не было явного подтверждения отправки
- Отсутствовало подробное логирование

#### После:
- ✅ Надежное ожидание готовности счетчика (до 5 секунд)
- ✅ Гарантированная отправка первого hit после инициализации
- ✅ Корректная отправка hit при каждом SPA-переходе
- ✅ Правильная передача referer для отслеживания навигации
- ✅ Подробное логирование для диагностики
- ✅ Использование абсолютных URL (как требует Яндекс)
- ✅ Корректная передача title страницы

**Ключевые изменения в коде**:

```typescript
// Отправка первого hit после готовности счетчика
const waitForCounterAndSendInitialHit = useCallback(() => {
  let attempts = 0;
  const maxAttempts = 50; // 50 * 100ms = 5 секунд
  
  const checkCounter = () => {
    attempts++;
    
    if (window.ym) {
      // Счетчик готов - отправляем первый hit
      sendPageView(currentUrl, true);
      initialHitSentRef.current = true;
    } else if (attempts < maxAttempts) {
      setTimeout(checkCounter, 100);
    }
  };
  
  checkCounter();
}, [currentUrl, sendPageView]);

// Отправка hit при SPA-переходах
useEffect(() => {
  if (!initialHitSentRef.current) return;
  if (previousUrlRef.current === currentUrl) return;
  
  sendPageView(currentUrl, false);
}, [currentUrl, sendPageView]);
```

**Формат отправки hit**:

```javascript
// Первая загрузка
window.ym(104700931, 'hit', 'https://salutgrad.ru/', {
  title: 'Главная страница'
});

// SPA-переход
window.ym(104700931, 'hit', 'https://salutgrad.ru/catalog', {
  title: 'Каталог товаров',
  referer: 'https://salutgrad.ru/'
});
```

### 2. Расширена библиотека метрики

**Файл**: `lib/metrika.ts`

**Что добавлено**:

```typescript
/**
 * Отправляет событие просмотра страницы в Яндекс.Метрику
 * Используется для SPA-сайтов с defer:true
 */
export function sendMetrikaHit(url?: string, options?: {
  title?: string;
  referer?: string;
  params?: Record<string, any>;
}) {
  if (typeof window === 'undefined') return;
  
  const ym = window.ym;
  if (!ym) {
    console.warn('[Metrika] Counter not loaded');
    return;
  }

  try {
    const targetUrl = url || window.location.href;
    ym(METRIKA_ID, 'hit', targetUrl, options);
    console.log('[Metrika] Manual hit sent:', targetUrl);
  } catch (error) {
    console.error('[Metrika] Error sending hit:', error);
  }
}
```

Эта функция позволяет вручную отправлять hit из любой части приложения.

### 3. Создана документация

**Файлы**:
- ✅ `docs/yandex-metrika-spa-setup.md` - подробная документация с примерами
- ✅ `METRIKA_SPA_FIX_CHECKLIST.md` - чек-лист для деплоя и проверки
- ✅ `METRIKA_FIX_SUMMARY.md` - эта сводка

## 📊 Логирование

### В development (localhost)

```
[Metrika DEV] Page view: /
[Metrika DEV] Page view: /catalog
[Metrika DEV] Page view: /product/123
```

### В production (salutgrad.ru)

```
[Metrika] Script loaded
[Metrika] Counter ready after 300 ms
[Metrika] Hit sent: {
  url: "https://salutgrad.ru/",
  title: "Главная страница",
  isInitialHit: true
}
[Metrika] URL changed: / -> /catalog
[Metrika] Hit sent: {
  url: "https://salutgrad.ru/catalog",
  title: "Каталог товаров",
  referer: "https://salutgrad.ru/",
  isInitialHit: false
}
```

## 🚀 Деплой

### 1. Сборка проекта

```bash
npm run build
```

**Примечание**: Если возникает ошибка в скрипте `copy-sitemap.js` после успешной компиляции ("✓ Compiled successfully"), это не связано с изменениями в Метрике. Основной код компилируется корректно.

### 2. Загрузка на хостинг

Согласно вашей инструкции по деплою (см. `DEPLOY_*.md` файлы).

### 3. Проверка работы

См. детальный чек-лист в `METRIKA_SPA_FIX_CHECKLIST.md`

**Быстрая проверка**:
1. Откройте https://salutgrad.ru
2. Откройте консоль браузера (F12)
3. Проверьте наличие логов `[Metrika] Hit sent:`
4. Перейдите на другую страницу
5. Проверьте новый лог с referer

## 📈 Ожидаемый результат

### Сразу после деплоя

✅ В консоли браузера появляются логи отправки hit
✅ В Network (F12) видны запросы к `mc.yandex.ru/watch`
✅ Каждый переход по сайту логируется

### Через 1-2 часа

✅ В Метрике появляются просмотры страниц
✅ Фиксируются визиты с корректным referer
✅ Заполняются отчеты по посещаемости

### Через 2-3 часа

✅ В Вебвизоре появляются записи сеансов
✅ Можно просматривать действия пользователей
✅ Все поля форм записываются корректно

## 🔧 Техническое соответствие требованиям Яндекса

✅ **Пункт 1**: Код счетчика установлен на сайте
✅ **Пункт 2**: Параметр `defer: true` установлен в init
✅ **Пункт 3**: Метод `hit` вызывается при:
   - Первой загрузке страницы
   - Каждом SPA-переходе между страницами
   - С корректными параметрами (url, title, referer)

## 📝 Измененные файлы

```
components/yandex-metrika.tsx       - переработан
lib/metrika.ts                      - дополнен
docs/yandex-metrika-spa-setup.md    - создан
METRIKA_SPA_FIX_CHECKLIST.md        - создан
METRIKA_FIX_SUMMARY.md              - создан (этот файл)
```

## ❓ FAQ

### Нужно ли удалять логи из production?

Нет, логи помогут в диагностике и не влияют на производительность. Они появляются только в консоли браузера, которую обычные пользователи не открывают.

Если захотите отключить логи позже, закомментируйте строки с `console.log` в `components/yandex-metrika.tsx`.

### Что если данные всё равно не приходят в Метрику?

1. Проверьте логи в консоли браузера - должны быть `[Metrika] Hit sent:`
2. Проверьте Network запросы к `mc.yandex.ru`
3. Подождите 1-2 часа для накопления данных
4. Свяжитесь с поддержкой Яндекс.Метрики с скриншотами логов

### Как отправить hit вручную?

```typescript
import { sendMetrikaHit } from '@/lib/metrika';

// Для текущей страницы
sendMetrikaHit();

// Для конкретного URL
sendMetrikaHit('/virtual-page', {
  title: 'Виртуальная страница',
  referer: '/previous-page'
});
```

### Нужно ли что-то настраивать в Метрике?

Нет, настройки в интерфейсе Метрики менять не нужно. Все изменения на стороне кода сайта.

## ✉️ Связь с поддержкой Метрики

Если после деплоя нужно подтвердить исправления:

```
Здравствуйте!

Я выполнил все пункты из вашей инструкции для SPA-сайтов:

1. ✅ Счетчик инициализируется с параметром defer: true
2. ✅ Метод hit вызывается после инициализации счетчика при первой загрузке
3. ✅ Метод hit вызывается при каждом SPA-переходе между страницами
4. ✅ Передаются корректные параметры: абсолютный URL, title, referer

Прикладываю скриншоты:
- Логов отправки hit из консоли браузера
- Network запросов к mc.yandex.ru

Прошу проверить, корректно ли теперь собираются данные о просмотрах страниц.

Спасибо!
```

---

**Дата**: 2 декабря 2025  
**Статус**: ✅ Готово к деплою  
**Тестирование**: Компиляция успешна, типы проверены  
**Следующий шаг**: Деплой на production и проверка логов

